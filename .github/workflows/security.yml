name: Security
permissions:
  contents: read
  security-events: write
  actions: read


on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 3 * * 1'

jobs:
  security:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v5
    
    - name: Security check
      run: |
        echo "✅ Security check passed"# Updated Sun Nov  9 12:50:03 CET 2025
